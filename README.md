# Navyug-C-assignment
We reverse both linked lists to make addition easier (least significant digit first). Then we perform digit-wise addition with carry, create a new list for the result, and reverse it again to restore original order. Finally, leading zeros are removed.
